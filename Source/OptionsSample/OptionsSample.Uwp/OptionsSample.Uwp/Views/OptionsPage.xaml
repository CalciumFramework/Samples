<Page
	x:Class="OptionsSample.Views.OptionsPage"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:OptionsSample"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d"
	Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

	<Page.Resources>
		<!--Use a collection view source for content that presents a list of items that can be grouped or sorted.-->
		<CollectionViewSource x:Key="cvs" x:Name="cvs" IsSourceGrouped="True" Source="{x:Bind ViewModel.Groupings}" />

		<local:OptionTemplateSelector 
			x:Key="optionsTemplateSelector" 
			Templates="{StaticResource OptionTemplateCollection}">
		</local:OptionTemplateSelector>
	</Page.Resources>

	<Grid>
		<ListView ItemsSource="{Binding Source={StaticResource cvs}}"
				ItemTemplateSelector="{StaticResource optionsTemplateSelector}"
				SelectionMode="None">
			<ListView.ItemContainerStyle>
				<Style TargetType="ListViewItem">
					<Setter Property="HorizontalContentAlignment" Value="Stretch" />
				</Style>
			</ListView.ItemContainerStyle>
		</ListView>
	</Grid>
</Page>
